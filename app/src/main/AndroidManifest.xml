<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itspeed.naidou" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_96"
        android:label="@string/app_name"
        android:name=".app.AppContext"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".app.activity.MainActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.ns.mutiphotochoser.GalleryActivity"
            android:theme="@style/ChoserTheme">

            <intent-filter>
                <!--***改成应用的包名-->
                <action android:name="com.itspeed.naidou.action.CHOSE_PHOTOS" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <activity android:name=".app.activity.SecActivity"
            android:theme="@style/ChoserTheme">
        </activity>

        <activity android:name=".app.activity.SettingActivity"
            >
        </activity>
        <activity android:name=".app.activity.SimpleBackActivity"
            >
        </activity>
        <activity android:name=".app.activity.PublishActivity"
            >
        </activity>
    </application>






    <!-- 权限-->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--SD卡读写权限-->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="ANDROID.PERMISSION.WRITE_EXTERNAL_STORAGE" />


</manifest>
